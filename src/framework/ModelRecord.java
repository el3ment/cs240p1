package framework;

/**
 * Generic class so the Model class can assume things about the children
 * 
 * @return				the table name for the model
 */
public class ModelRecord {
	public Integer id = -1;
}
